import logoImg from "@/assets/images/logo.svg";

const Logo = () => {
  return (
    <div className="logo">
      <img src={logoImg} alt="logo" className="logo" />
    </div>
  );
};

export default Logo;
